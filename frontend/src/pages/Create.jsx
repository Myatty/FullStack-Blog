/* eslint-disable no-unused-vars */
import PostForm from "../components/PostForm";

const Create = () => {
  return (
    <>
      <PostForm header={"Create Your Post Now!"} btnText={"Post"} method={"post"} />
    </>
  );
};

export default Create;
